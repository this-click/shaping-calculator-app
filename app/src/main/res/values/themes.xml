<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ShapingCalculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/green_light</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/green_dark</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Customize your theme here. -->
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/black</item>
    </style>

    <style name="Base.Theme.ShapingCalculator.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
</resources>